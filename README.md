# Simple User Info API

A simple Node.js API to display some of the user's info present on headers.


## Usage

- Accessing the route `api/whoami` will return the user's language, software and IP address.